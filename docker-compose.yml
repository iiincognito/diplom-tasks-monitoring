version: '3.8'

services:
  scheduler:
    build: .
    ports:
      - "7540:7540"
    environment:
      - TODO_PORT=7540
      - TODO_DBFILE=/data/scheduler.db
      - TODO_PASSWORD=${TODO_PASSWORD:-}
    volumes:
      - ./data:/data
    restart: unless-stopped
